#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
int main()
{
    int S, B, D, total;
    scanf("%d %d %d", &S, &B, &D);
    total = S + B + D;
    printf("%d\n", total);
    return 0;
}