#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
int main(){
    int n, m, contador, funcionario;
    scanf("%d %d", &n, &m);
    
    contador = 1;
    while (contador < n){
        contador += 1;
        scanf("%d", &funcionario);

    }
    return 0;
}